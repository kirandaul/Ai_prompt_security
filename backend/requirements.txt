fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Image scanning (OCR). Optional — the /api/scan-image endpoint degrades
# gracefully if these are absent.
rapidocr-onnxruntime>=1.2
Pillow>=10.0
numpy>=1.24

# Tier-2 deeper secret scan. Optional — degrades gracefully if absent.
detect-secrets>=1.5

# Document parsing - PDF, Word, Excel, CSV
pypdf>=4.0.1
python-docx>=0.8.11
openpyxl>=3.11.2
chardet>=5.2.0
